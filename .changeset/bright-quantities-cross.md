---
"effect-units": minor
---

Add `QuantityFromValue` for finite bare-number wires, quantity-aware positive/non-negative schema combinators and inclusive lower-bound checks, and `Quantity.fromBigDecimal` / `toBigDecimal` crossings with explicit precision and range guarantees.

Document custom-unit IDs and base scales as persistence contracts, safe cents-backed custom-unit boundaries, and how to wrap an existing Effect schema without losing its validation.
